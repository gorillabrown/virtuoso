{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/scripts/virtuoso_preflight.py\" --root \"${CLAUDE_PROJECT_DIR}\" --mode detect --quiet"
          }
        ]
      }
    ]
  }
}
